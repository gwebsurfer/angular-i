import { DateGreaterThanTodayDirective } from './date-greater-than-today.directive';

describe('DateGreaterThanTodayDirective', () => {
  it('should create an instance', () => {
    const directive = new DateGreaterThanTodayDirective();
    expect(directive).toBeTruthy();
  });
});
